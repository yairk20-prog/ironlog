{
  "name": "ironlog",
  "private": true,
  "type": "module",
  "description": "IRONLOG — the site itself has no build step; this file exists only so Netlify installs the one dependency the coach function uses for its usage quotas.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
