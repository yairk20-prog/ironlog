# HANDOFF — מצב הפרויקט

הקובץ הזה נכתב כדי להעביר את הפרויקט לשיחה חדשה שבה ה-repo מחובר.
כל ההיסטוריה (12 קומיטים) נמצאת כאן. אין צורך בשיחה הקודמת.

## מה צריך לעשות ראשון בשיחה החדשה

```bash
git clone ironlog-repo.bundle gym      # אם הגעת מקובץ bundle
cd gym
git remote add origin <כתובת ה-repo>   # ה-repo שמחובר לשיחה
git push -u origin main
```

אחרי ה-push: ב-Netlify → **Add new site → Import an existing project → GitHub** → לבחור
את ה-repo. `netlify.toml` כבר מגדיר publish directory ו-functions, אין build command.
רק אחרי חיבור ל-Git רצות ה-Netlify Functions (Netlify Drop לא מריץ אותן).

## מה כבר עובד

- PWA עברית RTL, ללא build step. כל מסך הוא מודול ES שמייצא `render(ctx)`.
- IndexedDB (`js/db.js`, DB_VERSION 2) — אימונים, סטים, תזונה, מדידות גוף, הגדרות.
- מנוע תוכנית PPL עם progressive overload ו-**כמה מטרות במקביל** (`resolveGoal` ב-`js/programs.js`).
- **`js/figure.js`** — הדגמת תרגיל מצוירת (23 דפוסי תנועה), לא GIF מהאינטרנט.
- **`js/feed.js`** — מצב פיד בהחלקה, כרטיס לכל תרגיל + כרטיס מנוחה.
- **`js/rest.js` / `js/timer.js`** — טיימר מנוחה עם דוק נגרר, כפתורי ‎−20/+20.
- תזונה מחולקת לארוחות + הנחיות לפי מטרה + מים בכוסות.
- `netlify/functions/coach.js` — מאמן AI דרך פרוקסי: **המפתח יושב אצל בעל האתר בלבד**,
  המשתמשים לא מזינים כלום. צריך להגדיר `ANTHROPIC_API_KEY` ב-Netlify → Environment variables.
- `netlify/functions/backup.js` + `js/cloud.js` — גיבוי ענן ללא חשבון, לפי קוד שחזור.
- דף הבית מציג כרטיס "מחר" — תצוגה מקדימה של יום האימון (או המנוחה) הבא בסבב, אחרי
  התרגילים של היום (`dayCard`/`restDayCard` המשותפים מ-`js/screen-plan.js`).
- זיהוי קלוריות מתמונה — `photoSheet` ב-`js/screen-nutrition.js` היה קיים ומחובר מההתחלה
  (`ai.analyzeMealPhoto`); מה שחסר היה עיצוב — הוחלף ה-`<input type=file>` הגולמי בכפתור
  מרובע מעוצב (`.photo-pick`, גם ב-`screen-body.js` לתמונת התקדמות), והתמונה נשארת על
  המסך גם כשהניתוח נכשל (במקום להיעלם עם השגיאה).
- הדגמת תרגיל: `js/media.js` מייצא `exerciseDemo()` שבוחר בין שתי תמונות אמיתיות
  (crossfade, ל-84 תרגילים שיש להם `TWO_FRAMES` — קוד שהיה קיים אך מעולם לא חובר) לבין
  הדמות המצוירת המקורית (fallback לשאר). מחובר במסך האימון, בנגן המוגדל, ובמצב הפיד.
- מצב פיד: תוקן אייקון רמז הכיוון שהיה הפוך (`chevronDown` כשהטקסט אמר "למעלה"), נוסף
  פס התקדמות לפי תרגיל (`​.feed-progress`) שמראה מה עבר/עכשיו/הבא-מיד לעומת מה שרחוק
  יותר, ותוקן באג RTL/bidi שהפך את מונה "1 / 12" ל-"12 / 1".
- כל שדה `<input type=file>` הוחלף בכפתור מרובע מעוצב עם אייקון (לא ה-widget הגולמי
  של הדפדפן) — התאמה עם עיצוב שאר האפליקציה.
- כל פריט מזון (ברשימה, בתצוגה המקדימה, בתוצאות AI) מציג אייקון צבעוני לפי קטגוריה
  (חלבון/חלב/פחמימה/ירק-פרי/שומן) — `food.js` מתייג כל ערך במילון, וסכמת ה-AI
  (`js/ai.js`) מבקשת את אותה קטגוריה גם למאכלים שזוהו על ידי AI.
- ניווט אחורה: לכל sheet יש עכשיו כפתור X גלוי בכותרת (בנוסף ל-grab handle), לא רק
  מחווה/הקשה על הרקע. ניווט בין מסכים (topbar back) כבר היה קיים לכל המסכים המשניים.
- הגדרות → "עיצוב": בחירת צבע הדגשה (7 גוונים), משתנה CSS יחיד (`--accent`) שצובע
  מחדש את כל האפליקציה מיידית. תוך כדי זה התגלה ותוקן באג אמיתי: `el()` ב-`ui.js`
  היה מפיל בשקט custom properties (`--foo`) דרך `Object.assign` על `style` — זה
  השבית גם את הצבעים בבורר המשקולות (`platePicker`) מאז שנכתב.

## מה פתוח

1. **המאמן/מחשבון המזון מחזירים 400** — עדיין לא נסגר, אבל כבר ניתן לאבחון.
   הבדיקה של הסבב הקודם העלתה שהמודל `claude-sonnet-5` **כן** תקף, ו-`temperature`
   אכן אינו חלק מה-API הנוכחי — כלומר שני התיקונים ההם נכונים אך לא היו הסיבה.
   ה-400 מגיע מ-Anthropic עצמה, והסיבות השכיחות הן **יתרת קרדיט אפסית** או
   **תקרת הוצאה** בחשבון ה-API — שתיהן מוחזרות כ-400 עם הסבר בגוף התשובה, שהפונקציה
   קודם בלעה והחזירה רק "שגיאת AI (400)". עכשיו `explain()` ב-`netlify/functions/coach.js`
   מתרגמת את הסיבה לעברית, ו-`GET /api/coach?check=1` מריץ קריאה אמיתית אחת ומחזיר
   את התוצאה. בהגדרות → "מאמן AI" יש כפתור **"בדוק חיבור לשרת ה-AI"** שמדפיס את זה
   על המסך. **צעד ראשון בכל דיווח עתידי: ללחוץ על הכפתור הזה ולקרוא מה כתוב.**
2. השוואה מול אפליקציות כושר מובילות — אין גישת רשת בסביבה הזו לאתרים חיצוניים.

### פיצ'רים ותיקונים מסבב נוסף (בקשות המשתמש)

- הדגמת תרגיל: `exerciseDemo()` (ב-`js/media.js`) מוצגת עכשיו גם במסך המנוחה
  (`rest.js`) ובכרטיס המנוחה בפיד — תוקן גם באג CSS אמיתי: `.rest-next-fig` לא
  היה לו גודל מוגדר בכלל (עבד "במקרה" עם ה-SVG המצויר).
- מסך המנוחה וכרטיס המנוחה בפיד מציגים עכשיו גם הערת "שמרת בפעם הקודמת" (מ-
  `getNote`/`session.js`) ותזכורת שתייה, לצד הדגש הקיים.
- צליל בסיום המנוחה (`chime()` ב-`js/ui.js`, Web Audio) — נשמע גם עם אוזניות
  ומוזיקה בווליום מלא, כי הוא מתערבב לאותו פלט שמע ולא עוצר את המוזיקה. הגדרה
  חדשה `soundAlert` (ברירת מחדל: פעיל).
- Wake Lock (`timer.js`) — המסך לא נכבה/ננעל מעצמו כל עוד טיימר מנוחה רץ, כך
  שאין צורך לפתוח את הנייד עם סיסמה רק כדי לבדוק כמה זמן נשאר. **מגבלה אמיתית**:
  אין דרך ל-PWA להציג תוכן על מסך נעילה של ממש או לפעול אחרי שה-OS השהה את הדף
  לגמרי — Wake Lock מונע את הכיבוי האוטומטי, אבל לא "עוקף" נעילה יזומה. נוספה גם
  התראת רקע אופציונלית (Notification API, מכבה כברירת מחדל, דורשת הרשאה).
- אימון ספונטני: כפתור חדש בדף הבית ("אימון ספונטני") לפתיחת יציבה/מתיחות או
  תרגיל חופשי לפי חיפוש, בכל זמן — לא רק ביום מנוחה. **תיקון באג אמיתי**: התחלת
  יום אחר בסבב (מסך "תוכנית") הייתה מזיזה את הסבב השבועי גם כשלא באמת התאמנת
  ביום המתוכנן — עכשיו לאימונים "מחוץ לתוכנית" יש דגל `offPlan` שמונע מהם להזיז
  את הסבב.

## בדיקות

```bash
node tools/run-all.mjs            # הכל: fn-test + 12 חבילות e2e, מריץ שרת משלו
python3 tools/build-icon.py       # מייצר מחדש את הלוגו והאייקונים
node tools/audit.mjs              # סופר כפתורים לכל מסך (שמירה על ממשק רזה)
```

`run-all.mjs` מפעיל ומכבה את `serve.mjs` בעצמו בשלושה מצבים (`COACH=1`, בלי מאמן,
ו-`COACH=fail` — מפתח מוגדר אך ה-API מסרב), כי כמה חבילות בודקות דווקא את המצבים
האלה. הרצה ידנית מול שרת שבמקרה רץ היא הדרך לקבל ירוק חסר משמעות; הרנר מסרב
להתחיל אם הפורט תפוס.

**הערה על אמינות הבדיקות** (תוקן בסבב הזה, היה שבור קודם):
- החבילות סופרות שגיאות קונסול ככישלון, וגופן Google חסום כאן — כל בקשה חיצונית
  נענית ריקה דרך `localOnly()` ב-`tools/launch.mjs`.
- Chromium פונה מעצמו ל-Google (autofill, component update). בסביבה שבה ה-egress
  *תולה* במקום לסרב, זה הקפיא חבילות לדקות. `LAUNCH` ב-`tools/launch.mjs` מכבה אותן.
- `process.exit()` קוטע כתיבה ל-pipe, ולכן שורת ה-`FAIL:` נעלמה בכל הרצה דרך רנר.
  כל החבילות משתמשות ב-`process.exitCode`.
- `e2e.mjs`..`e2e5.mjs` לא מדפיסות `PASS` בכלל — הקריטריון הוא קוד היציאה.

## מגבלות סביבה שנתקלנו בהן

- הורדת GIF-ים של תרגילים מהאינטרנט חסומה (403 מהפרוקסי) — לכן הדמות מצוירת בקוד.
- אין להעתיק GIF-ים מאתרים מסחריים; הדמות ב-`figure.js` מקורית לחלוטין.
- ה-egress כאן תולה חיבורים חסומים במקום לסרב מיד; כל כלי שמדבר החוצה צריך timeout.


## סבב 5.2 — שני הדיווחים מהנייד

**1. ה-400 של ה-AI.** ראה "מה פתוח" למעלה. בקצרה: הפונקציה החזירה רק קוד סטטוס,
והסיבה האמיתית (שברוב המקרים היא יתרת קרדיט או תקרת הוצאה בחשבון ה-API) נשארה
בלוגים של Netlify שאף אחד לא קורא מהנייד. עכשיו הסיבה מגיעה למסך, עם כפתור בדיקה.
הודעות שגיאה מ-upstream מועברות רק עבור סוגי שגיאה מוכרים, וכל מה שנראה כמו מפתח
מנוקה (`scrub()`), כך שגוף שגיאה לא-מוכר לעולם לא מועבר כמו שהוא.

**2. האפליקציה לא מתעדכנת בנייד — נמצא השורש.** ה-service worker אכן עבר
network-first בסבב הקודם, אבל `netlify.toml` הגיש את `/js/*` ו-`/css/*` עם
`Cache-Control: max-age=31536000, immutable`. הקבצים אינם ממוספרים (תמיד `app.js`),
כך שכל טלפון שכבר ביקר באתר קיבל הוראה לשמור את הגרסה ההיא **לשנה** ולא לשאול שוב —
ו-`fetch()` של ה-worker נענה מה-HTTP cache של הדפדפן לפני שהגיע לרשת. כלומר
ה-network-first לא באמת יצא לרשת אף פעם. תוקן בשתי רמות:
- הכותרות ל-`/js/*` ו-`/css/*` הן עכשיו `max-age=0, must-revalidate` (ETag → 304).
- ה-worker מבקש קוד עם `cache: 'reload'` (`fresh()` ב-`sw.js`), כולל ב-precache —
  זו דרך המילוט היחידה למכשיר שכבר אחסן את הקבצים הישנים כ-immutable.
- `js/version.js` מחזיק `BUILD`, והגדרות → "גרסה ועדכונים" מציגות את גרסת הדף
  מול הגרסה שה-worker מדווח, עם כפתור "אלץ עדכון מלא" שמנקה caches ומבטל רישום.
  `e2e10.mjs` נכשל אם `sw.js` ו-`version.js` יוצאים מסנכרון.
- `app.js` כבר לא מרענן את הדף כשה-worker נרשם **בפעם הראשונה** (אין controller
  קודם) — הרענון ההוא היה קוטע את שאלון הפתיחה באמצע.

**3. ביקורת על הסבב הקודם.** כל 13 הפיצ'רים שנטענו ב-HANDOFF נבדקו מול הקוד.
12 מומשו וחווטו במלואם. אחד היה חלקי ותוקן: ההגדרה `bgNotify` נשמרה והוצגה
בהגדרות אבל `notify()` ב-`timer.js` מעולם לא קרא אותה — היא בדקה רק את הרשאת
הדפדפן, כך שכיבוי ההגדרה לא עשה כלום. עכשיו `setBgNotify()` מחווט דרך `app.js`.
בנוסף, `tools/fn-test.mjs` **לא עבר** בסבב הקודם (הוא עדיין בדק את שם המודל הישן
ואת `temperature`) — הטענה "כל החבילות עוברות" לא הייתה נכונה. עודכן ועובר.


## סבב 5.3 — אימון ספונטני, ולוגו

**1. האימון הספונטני לא נעלם — הוא פשוט לא היה נראה.** הוא היה קיים ככפתור
טקסט דהוי אחד (`class: 'linkish'`) מתחת ל-hero, מה ששווה ערך לכך שלא היה קיים.
עכשיו יש ארבע אריחים מסומנים תחת הכותרת "בלי קשר לתוכנית":

| אריח | מה הוא עושה |
| --- | --- |
| יציבה | תבנית `posture` הקיימת |
| מתיחות | תבנית `stretch` **חדשה** — רק אחיזות, 5 דקות, בלי אקטיבציה ובלי core |
| אתגר | `CHALLENGES` **חדש** — סט יחיד עד כישלון, מול השיא הקודם שלך |
| חופשי | חיפוש תרגיל; נפתח על תרגילי משקל גוף במקום על רשימה ריקה |

האתגרים (`CHALLENGES` ב-`programs.js`, `createChallenge`/`challengeBest` ב-`session.js`)
הם משקל גוף בלבד בכוונה — אפשר לעשות אותם בכל מקום. התוצאה נשמרת כסט רגיל,
כך שיומן הסטים הקיים הוא ספר השיאים ולא צריך אחסון חדש.

**כולם `offPlan`** ולכן אף אחד מהם לא מזיז את הסבב השבועי. `e2e11.mjs` בודק את
זה — וחשוב מכך, יש בו **בקרה**: הוא מוודא שאימון *מתוכנן* כן מזיז את הסבב
(0 ← 1), אחרת כל הבדיקות של "לא זז" עוברות מול מונה שלא זז אף פעם.

**2. לוגו.** `tools/build-icon.py` מייצר את הכל מגיאומטריה אחת: קו מתאר אחד
בכתב יד, וכל השאר הצללה בתוכו. ההצללה נוצרת (קווים מקבילים בזווית שעוקבת
אחרי הצורה, שני מעברים שמצטלבים), ולא מצוירת ביד. הסגנון הוא עיפרון כחול
לפי התמונה שהמשתמש שלח — **צויר מאפס באותו סגנון, לא הועתק**.

שתי פלטות מאותו ציור. היפוך של רישום עיפרון הוא לא החלפה של שני צבעים: על נייר
הטון הוא גרפיט *שמוסיפים* ללבן, על רקע כהה הטון הוא אור *שמוסיפים* לשחור — ולכן
מעבר הצללים ומעבר ההדגשות מתחלפים. הכהה הוא מה שנשלח (`THEME = 'dark'`),
הבהיר נשמר ב-`icons/logo-light.svg` לדף מודפס או רקע לבן.

רישום עיפרון לא שורד הקטנה לאייקון — ההצללה נמרחת מתחת ל-96px בערך — ולכן
אותה גיאומטריה בונה גם `flat()`: בלי הצללה, קו מתאר עבה, ומזה נחתכים ה-PNG-ים
הקטנים. `e2e11.mjs` נכשל אם קובץ אייקון חסר, אם אין `maskable` ב-manifest, או
אם ה-`apple-touch-icon` לא מצביע על 180px.


## סבב 5.4 — לוגו שני, פארק/בית, אורך אימון

**1. הלוגו הוחלף שוב** — המשתמש שלח תמונת ייחוס אחרת (זרוע כפופה אוחזת
במשקולת, סגנון שטוח וצבעוני, לא עיפרון). `tools/build-icon.py` נכתב מחדש
מאפס: אותו קו-מתאר של הזרוע (לא הועתק — הגיאומטריה כבר הייתה טובה, רק
המילוי השתנה), הצללה רכה בקשתות שקופות במקום קווקוו, ומשקולת (שני עיגולים
וקו) עם ה-hub בצבע ה-accent של האפליקציה (`#FF5C00`) כנגיעת מותג. הסגנון
הקודם (עיפרון) הוסר לגמרי — שתי הפסקאות למעלה מתארות אותו ולא את הקיים.

**שינוי טכני**: אין `sharp` בסביבה הזו. הרסטריזציה עברה ל-Playwright (כבר
מותקן בשביל ה-e2e) — דף headless טוען כל SVG בגודל CSS מדויק וצילום מסך שלו,
באמצעות ה-Chromium שכבר קיים ב-`/opt/pw-browsers/chromium`.

**2. מטמון immutable על `/icons/*` היה מונע מעדכוני אייקון להגיע.** המשתמש
דיווח שאייקון האפליקציה במסך הבית לא השתנה אחרי הדיפלוי. הסיבה: `netlify.toml`
הגיש את `/icons/*` עם `max-age=31536000, immutable` בהנחה ש"אייקונים לא
משתנים" — נכון כל עוד שם הקובץ לא משתנה כשהתוכן כן. בדיוק אותו באג שתוקן
בסבב 5.2 עבור `/js/*` ו-`/css/*`, באותו קובץ, שנשאר בלי טיפול עבור האייקונים.
עודכן לאותה מדיניות (`max-age=0, must-revalidate`). **הערה ל-iOS**: גם אחרי
התיקון, אייקון שכבר נוסף למסך הבית דרך "הוסף למסך הבית" לא מתעדכן לבד —
זו תכונה של iOS (צילום חד-פעמי בזמן ההוספה), לא באג באפליקציה; הדרך היחידה
היא להסיר ולהוסיף מחדש.

**3. אימוני פארק ובית.** שתי תבניות חדשות ב-`TEMPLATES` (`programs.js`):
`park` (מתח, מקבילים, סקוואט משקל גוף, עליית מדרגה, הרמת רגליים בתלייה,
פלאנק — כל התרגילים דורשים רק מתקן פארק סטנדרטי) ו-`home` (שכיבות סמיכה,
חתירה הפוכה, סקוואט ומכרעים במשקל גוף, גשר ישבן, פלאנק — בלי מוט בכלל).
ארבעה תרגילים חדשים נוספו ל-`exercises.js` כדי שזה יעבוד בלי פשרות:
`bw_squat`, `bw_lunge`, `pike_pushup`, `inverted_row` — כולם `cat: 'Bodyweight'`.
שני אריחים חדשים בדף הבית ("פארק"/"בית"), אותו דפוס `offPlan` כמו יציבה/מתיחות.

**4. אורך אימון.** הגדרה חדשה `settings.duration` (`DURATIONS` ב-`programs.js`:
זריז/קצר/בינוני/ארוך) קובעת כמה תרגילים מתוך התבנית נכנסים לאימון וכמה
סטים לכל תרגיל — `createFromTemplate` ב-`session.js` חותך את `tpl.slots`
לפי ההגדרה **לפני** הבנייה, לכן זה חל על כל תבנית (הסבב הרגיל, פארק, בית,
יציבה, מתיחות כאחד) בלי קוד נפרד לכל אחת. תבניות כתובות מורכבים-קודם בכוונה
בדיוק בשביל זה — קיצור מוריד בידוד מהסוף, לא את התרגיל שהיום בנוי סביבו.
מסך ההגדרות מקבל מקטע "אורך אימון" חדש; דף הבית (hero + "התרגילים היום")
משתמש באותה חיתוך כדי שהתצוגה המקדימה לעולם לא תבטיח יותר ממה שלחיצה על
"התחל" באמת תבנה.

בנוסף — לא הגדרה קבועה אלא כפתור: אריח "זריז" חדש בדף הבית בונה את היום
המתוכנן הבא (לא איזה תבנית שרירותית) באורך `quick` ו-`offPlan: true`
(`createQuickWorkout` ב-`session.js`) — אימון אמיתי של כ-10 דקות שלא צורך
את היום המתוכנן, כך שהגרסה המלאה עדיין מחכה מתי שיהיה זמן. ביום מנוחה,
כשאין "הבא" לקצר, הוא נופל חזרה על תבנית `stretch`.

`e2e11.mjs` עודכן: 7 אריחים (במקום 4), עם בדיקה מפורשת לכל אחד מהשלושה
החדשים בשמו.
